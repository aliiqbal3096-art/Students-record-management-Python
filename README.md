# Students-record-management-Python
A command-line Student Management System built with Python that performs CRUD operations (Create, Read, Update, Delete) on student records and stores data permanently using JSON file handling.
